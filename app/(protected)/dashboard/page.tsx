import Chart from "@/components/Chart";
import ProjectsList from "@/components/ProjectsList";

const page = async () => {
  return (
    <div>
      <Chart />
      <ProjectsList />
    </div>
  );
};

export default page;
